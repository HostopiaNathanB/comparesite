Used to compare a webpage that is seen live, with one that is stored on a different server, can also be used to see the Differences between the CDN and actual server. Used with 

`comparesite $url $ip`
